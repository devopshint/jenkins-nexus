---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: java_app
  namespace: default
  labels:
    app: java_app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java_app
  template:
    metadata:
      labels:
        app: java_app
    spec:
      containers:
      - name: java_app
        image: "devopshint/my-app-1.0:latest"
        ports:
          - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: java_app
  namespace: default
spec:
  selector:
    app: java_app
  type: LoadBalancer
  ports:
  - name: http
    targetPort: 8081
    port: 80